# -*- mode: sh; eval: (sh-set-shell "bash") -*-

# this names your "repo" and is reserved for other identification purposes
AMI_USER=${USER:-ami}
# above is prolly too dynamically brave, oh well
#AMI_USER=

# arch SVN stuff
SVN_SERVER=svn.archlinux.org
SVN_REPOS="packages community"

# const README file to seed initial master commit
MASTER_README="$AMI_VARS/master.README.md"

# init pkg with PKGBUILD{,-*}.proto file
PCM_SHARE=/usr/share/pacman
